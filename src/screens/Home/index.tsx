import React, { FC } from 'react';
import { Container, Text } from './styles';

type Props = {};

const Home: FC<Props> = () => (
  <Container>
    <Text>HOME</Text>
  </Container>
);

export default Home;
